@model IEnumerable<FreshAir.Models.Athlete>

@{
    ViewData["Title"] = "Athletes You Follow";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<h1>Athletes You Follow</h1>

@foreach (var item in Model)
{
    <div class="card" style="width: 25rem;">
        <img src="~/images/@item.ProfilePicture" class="card-img-top" alt="ProfilePicture">
        <div class="card-body">
            <p class="card-text">
                <div>
                    <h4>
                        @item.FirstName @item.LastName
                    </h4>
                </div>
                <div>
                    <a asp-action="FriendDetails" asp-route-id="@item.AthleteId"class="btn btn-primary">View Info</a>
                </div>
                <div>
                    <a asp-action="Unfollow" asp-route-id="@item.AthleteId"class="btn btn-primary">Unfollow This Athlete</a>
                </div>
            </p>
        </div>
    </div>
}