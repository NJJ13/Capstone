@model IEnumerable<FreshAir.Models.Event>

@{
    ViewData["Title"] = "Events You're Hosting";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<h1>Events You're Hosting</h1>

<p>
    <a asp-action="EventsAttending" class="btn btn-primary">Events You're Attending</a> |
    <a asp-action="PastHostedEvents" class="btn btn-primary">Events You've Hosted</a>
</p>
<p>
    
</p>

@foreach (var item in Model)
{
    <div class="card">
        <div class="card-header">
            @item.Activity - @item.LocationsName
        </div>
        <div class="card-body">
            <h3 class="card-title">@item.ScheduledTIme</h3>
            <p class="card-text">@item.Description</p>
            <a asp-action="MyEventDetails" asp-route-id="@item.EventId" class="btn btn-primary">Event Info</a>
            <a asp-action="EditEvent" asp-route-id="@item.EventId" class="btn btn-primary">Edit Event</a>
        </div>
    </div>
}


